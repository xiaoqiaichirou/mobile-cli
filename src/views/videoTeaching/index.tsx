import {defineComponent, reactive, ref } from "vue";

export default defineComponent({
    name: 'videoTeaching',
    setup() {
        const formParams = reactive({})
        return () => (
            <div class="container">
                videoTeaching
            </div>
        )
    }
})
